apiVersion: apps/v1
kind: Deployment
metadata:
  name: quantis-frontend
  labels:
    app: quantis-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: quantis-frontend
  template:
    metadata:
      labels:
        app: quantis-frontend
    spec:
      containers:
        - name: quantis-frontend
          image: registry.example.com/app/frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            limits:
              cpu: 300m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          env:
            - name: REACT_APP_API_URL
              value: /api
            - name: REACT_APP_ENV
              value: dev
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
      imagePullSecrets:
        - name: regcred
