apiVersion: v1
kind: Service
metadata:
  name: quantis-database
  labels:
    app: quantis-database
spec:
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
      name: mysql
  selector:
    app: quantis-database
